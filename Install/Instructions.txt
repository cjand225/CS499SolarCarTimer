#Make sure conda-forge is the top channel
conda config --add-channels conda-forge

#To Create an Env for sharing with the same OS
conda list --explicit > envBuild.txt

#To Install Existing env from file
conda create -n <env-name> --file envBuild.txt
conda activate <env-name>
pip install opencv-contrib-python
pip install pytimeparse


#packages required to run
Python3 = 3.7 or above
PyQt5 = 5.6 or above
Opencv-contrib-python = 3.4.3 or above
numpy = 1.5.4 or above
matplotlib = LTS or above
scipy = LTS or above

#for Vision Module
tesseract
imutils

Windows specfic:
pyWin32 = latest version



#For Windows


#For Mac